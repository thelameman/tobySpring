<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.2.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing 
		infrastructure -->

	<!-- Enables the Spring MVC @Controller programming model -->

	<!-- Handles HTTP GET requests for /resources/** by efficiently serving 
		up static resources in the ${webappRoot}/resources directory -->
	<mvc:annotation-driven />
	<mvc:resources mapping="/resources/**" location="/resources/" />
	<mvc:resources mapping="/upload/**" location="/upload/" />
	<mvc:resources mapping="/img/**" location="/img/" />

	<!-- Resolves views selected for rendering by @Controllers to .jsp resources 
		in the /WEB-INF/views directory -->
	<beans:bean
		class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".jsp" />
	</beans:bean>

	<context:component-scan base-package="com.buyingliving.app" />

	<beans:bean id="adminInterceptor"
		class="com.buyingliving.app.interceptors.AdminInterceptor">

	</beans:bean>

	<beans:bean id="loginInterceptor"
		class="com.buyingliving.app.interceptors.LoginInterceptor">
	</beans:bean>
	<mvc:interceptors>
		<mvc:interceptor>
			<mvc:mapping path="/approveBrokerList" />
			<mvc:mapping path="/approvePropertyList" />
			<mvc:mapping path="/QnAListView" />
			<beans:ref bean="adminInterceptor" />
		</mvc:interceptor>

		<mvc:interceptor>
			<mvc:mapping path="/myPage" />
			<mvc:mapping path="/cartView" />
			<mvc:mapping path="/insertPropertyView" />
			<mvc:mapping path="/freeBoardWriteView" />
			<mvc:mapping path="/insertFreeReply" />
			<mvc:mapping path="/insertQnaReply" />
			<mvc:mapping path="/qnaBoardWriteView" />
			<mvc:mapping path="/msgBoxView" />
			<mvc:mapping path="/addCart" />
			
			<beans:ref bean="loginInterceptor" />
		</mvc:interceptor>
	</mvc:interceptors>
</beans:beans>


